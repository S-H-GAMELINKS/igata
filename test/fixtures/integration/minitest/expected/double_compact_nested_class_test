# frozen_string_literal: true

require "test_helper"

class App::Model::User::ProfileTest < Minitest::Test
  def test_initialize
    # Arguments:
    #   - name (required)
    #   - age (required)
    skip "Not implemented yet"
  end

  def test_adult?
    # Comparisons: >= (@age >= 18)
    # Boundary value suggestions:
    #   @age >= 18: [17 (below), 18 (boundary), 19 (above)]
    skip "Not implemented yet"
  end

  def test_greeting
    skip "Not implemented yet"
  end
end
